/* eslint-disable import/no-extraneous-dependencies */
import React from 'react';
import { ClipLoader } from 'react-spinners';

const LoadingSpinner = () => {
	return <ClipLoader color="white" size={24} />;
};

export default LoadingSpinner;
